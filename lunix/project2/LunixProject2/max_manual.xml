<?xml version="1.0" ?>
<CommandManual>
  <description>Help on built-in function max in module builtins:

max(...)
    max(iterable, *[, default=obj, key=func]) -&gt; value
    max(arg1, arg2, *args, *[, key=func]) -&gt; value
    
    With a single iterable argument, return its biggest item. The
    default keyword-only argument specifies an object to return if
    the provided iterable is empty.
    With two or more arguments, return the largest argument.

</description>
  <version>Python 3.11.2</version>
  <example>No example available.</example>
  <related_commands>ge, iter, def</related_commands>
  <syntax>max(...)
 max(iterable, *[, default=obj, key=func]) -&gt; value
 max(arg1, arg2, *args, *[, key=func]) -&gt; value</syntax>
  <documentation_link>https://docs.python.org/3/</documentation_link>
</CommandManual>
