<?xml version="1.0" ?>
<CommandManual>
  <description>Help on built-in function print in module builtins:

print(*args, sep=' ', end='\n', file=None, flush=False)
    Prints the values to a stream, or to sys.stdout by default.
    
    sep
      string inserted between values, default a space.
    end
      string appended after the last value, default a newline.
    file
      a file-like object (stream); defaults to the current sys.stdout.
    flush
      whether to forcibly flush the stream.

</description>
  <version>Python 3.11.2</version>
  <example>print('Hello, World!')</example>
  <related_commands>append, str, def, int</related_commands>
  <syntax>print(*args, sep=' ', end='\n', file=None, flush=False)</syntax>
  <documentation_link>https://docs.python.org/3/</documentation_link>
</CommandManual>
